public Boolean isPrime(Integer num) {
    Boolean res=false;
    for(integer i=2;i<(num/2);i++)
    {
        Integer rem=math.mod(num,2);
        if(res==0){
            res=true;
            break;
        }
    }
    return res;
}